import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  baseView: {
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  cancelButton: {
    position: 'absolute',
    top: '0%',
    right: '0%',
    marginTop: 8,
    marginRight: 8,
  },
});

export {styles};
